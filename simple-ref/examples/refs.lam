-- x = (\x:Bool->Bool. x) (\y:Bool. y);

-- y = (\x:(Nat, Nat). succ (x.1)) (2, 3);

c = ref 0;

decc = \_:(). (c := pred !c; !c);

incc = \_:(). (c := succ !c; !c);

-- main = incc ()

-- incc ();
-- incc ();

-- decc ();
